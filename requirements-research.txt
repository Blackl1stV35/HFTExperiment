# ── HFTExperiment Research / Regime Notebook environment ──────────────────────
# Install: pip install -r requirements-research.txt
# Purpose: 00_market_regime_explorer.ipynb and all macro/regime analysis.
# Kept separate from main env to avoid torch/gymnasium version conflicts.
#
# Recommended setup:
#   python -m venv .venv-research
#   source .venv-research/bin/activate   (or .venv-research\Scripts\activate on Windows)
#   pip install -r requirements-research.txt
#   jupyter lab

# Notebook runtime
jupyter>=1.0.0
jupyterlab>=4.0.0
ipykernel>=6.27.0
ipywidgets>=8.1.0

# Data sources
yfinance>=0.2.36
pandas-datareader>=0.10.0      # FRED, World Bank
requests>=2.31.0

# Data wrangling
numpy==1.26.4                  # Pin to NumPy 1.x for compatibility
pandas>=2.2.0
polars>=0.20.0
pyarrow>=14.0.0
scipy>=1.11.0
statsmodels>=0.14.0

# Technical analysis
# Replaced pandas-ta with ta (compatible with NumPy 1.x)
ta>=0.11.0
vectorbt>=0.24.0               # optional, advanced TA + backtesting

# Regime / ML
scikit-learn>=1.3.0
hmmlearn>=0.3.0                # Hidden Markov Model regime detection
umap-learn>=0.5.5              # UMAP embedding for regime visualisation

# Visualisation
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0
kaleido>=0.2.1                 # plotly static export

# Utilities
loguru>=0.7.0
tqdm>=4.66.0
python-dotenv>=1.0.0
