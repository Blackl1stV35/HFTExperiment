# ── HFTExperiment TA environment ──────────────────────────────────────────────
# Install: pip install -r requirements-ta.txt
# Purpose: data download, feature engineering, backtesting, and RL training.
# Separate from requirements-research.txt (notebook/regime analysis env).

# Data sources
yfinance>=0.2.36
MetaTrader5>=5.0.45; sys_platform == "win32"

# Data wrangling
numpy>=1.26.0
pandas>=2.2.0
polars>=0.20.0
pyarrow>=14.0.0
duckdb>=0.9.0

# Technical analysis
pandas-ta>=0.3.14b

# ML core
torch>=2.1.0
scikit-learn>=1.3.0
gymnasium>=0.29.0

# Config & experiment tracking
hydra-core>=1.3.0
omegaconf>=2.3.0
wandb>=0.16.0
python-dotenv>=1.0.0

# Utilities
loguru>=0.7.0
tqdm>=4.66.0
requests>=2.31.0
joblib>=1.3.0
matplotlib>=3.8.0

# NLP (sentiment branch — optional, disable in config if not needed)
transformers>=4.35.0
tokenizers>=0.15.0

# Inference
onnxruntime>=1.16.0
onnx>=1.15.0

# Monitoring
prometheus-client>=0.19.0
